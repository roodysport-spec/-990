# نظام المراقبة الذكي للامتحانات - نسخة أندرويد

## المرحلة 1 ✅ (أساس المشروع)
- مشروع Android Studio كامل (Kotlin + CameraX)
- واجهة تحاكي برنامجك الأصلي: فيديو حي + لوحة تحكم + إحصائيات + سجل أحداث
- زر بدء/إيقاف مراقبة يفتح ويغلق الكاميرا فعلياً

## المرحلة 2 ✅ (كشف الوجوه + الاتجاه + التتبع + التنبيهات)
- **CentroidTracker.kt**: ترجمة حرفية لـ centroidtracker.py - يعطي كل طالب رقم ثابت
- **HeadPoseEstimator.kt**: حساب اتجاه الرأس (يمين/يسار/أعلى/أسفل) بنفس معادلة solvePnP بالبايثون، عبر مكتبة OpenCV
- **FaceMonitor.kt**: يربط MediaPipe FaceLandmarker (وضع LIVE_STREAM) مع التتبع وحساب الاتجاه
- **ImageUtils.kt**: يحوّل إطار الكاميرا (YUV) إلى Bitmap قابل للاستخدام
- **OverlayView.kt**: يرسم مربعات الطلاب فوق الفيديو (أخضر = طبيعي، برتقالي = التفات مستمر)
- **AlertManager.kt**: نظام التنبيه (تبريد 4 ثواني)، حفظ صور الأدلة، صوت تنبيه (ToneGenerator)، وتصدير تقرير CSV عند الإيقاف

**النتيجة**: التطبيق هلأ يكدر يكشف الوجوه بالكاميرا الحية، يتتبع كل طالب برقم ثابت، يحسب اتجاه نظره، ويطلق تنبيه + يحفظ صورة دليل + يسجل بالتقرير لو صار "التفات مستمر" — بالضبط نفس منطق `gui_app.py` الأصلي لهذا الجزء.

### وين تنحفظ الملفات على الجهاز؟
- الأدلة: `Android/data/com.skyline.proctor/files/Pictures/Evidence/`
- التقارير: `Android/data/com.skyline.proctor/files/Reports/`

## المرحلة 3 (الجاية) - لسا ناقصة
- كشف الممنوعات: موبايل/كتاب/لابتوب (MediaPipe ObjectDetector + نفس فكرة تقسيم الصورة Tiling)
- كشف الصوت (AudioRecord) بدل sounddevice
- إعدادات القاعة الكبيرة القابلة للتبديل من الواجهة (مثل hall_mode_switch الأصلي)
- دعم كاميرا خارجية/RTSP إذا احتجتها لاحقاً

## كيف تفتح المشروع

1. حمّل **Android Studio** (أحدث نسخة) من الموقع الرسمي: https://developer.android.com/studio
2. افتح Android Studio → **Open** → اختر مجلد `ProctorAndroid` هذا كامل
3. خلي البرنامج يسوي Sync تلقائي لأول مرة (يحمل مكتبات Gradle، يحتاج نت)
4. وصّل الجهاز (السبورة التفاعلية) بالكيبل USB وفعّل **وضع المطور + USB Debugging** من إعدادات أندرويد بالجهاز (Settings → About device → اضغط "Build number" 7 مرات لتفعيل وضع المطور)
5. اضغط ▶️ Run بـ Android Studio واختار جهازك من قائمة الأجهزة المتصلة

## ملاحظة مهمة
ملفات `face_landmarker.task` و`efficientdet_lite0.tflite` اللي عندك بالمشروع القديم (بايثون) نفسهم راح نستخدمهم بالمرحلة الجاية — بس لازم تحطهم بمجلد `app/src/main/assets/` (لسا ما موجود، راح نضيفه بالمرحلة الجاية).

## الخطوة الجاية
قلي جاهز تكمل، ونبدأ بالمرحلة 2: كشف الوجوه واتجاه النظر (أهم ميزة بالبرنامج).
